{
  "name": "telemetry-frontend",
  "version": "0.1.0",
  "description": "Telemetry Frontend",
  "repository": {
    "type": "git",
    "url": "https://github.com/CIUDSE/ciudse-telemetry.git"
  },
  "private": true,
  "scripts": {
    "fetch-openmct": "if cd openmct; then git pull; else git clone https://github.com/nasa/openmct.git; fi",
    "build-openmct": "cd openmct && npm install && npm run build:prod && cd ..",
    "install-openmct": "mkdir -p ./dist/openmct && rsync -a --delete ./openmct/dist/ ./dist/openmct",
    "get-openmct": "npm run fetch-openmct && npm run build-openmct && npm run install-openmct",
    "dev:clean": "npm run get-openmct && npm run dev",
    "dev": "npm run lint && parcel serve src/index.html",
    "build": "npm run lint && npm run get-openmct && parcel build src/index.html",
    "test": "",
    "lint": "eslint ./src/**/*.js",
    "lint:fix": "eslint --fix ./src/**/*.js"
  },
  "keywords": [],
  "author": "Club de Investigación Universitario de Desarrollo en Sistemas Espaciales",
  "devDependencies": {
    "@parcel/validator-eslint": "^2.0.1",
    "eslint": "^7.32.0",
    "eslint-config-standard": "^16.0.3",
    "eslint-plugin-import": "^2.25.2",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^5.1.1",
    "parcel": "^2.0.0",
    "parcel-resolver-ignore": "^2.0.0"
  },
  "engines": {
    "node": "14.18"
  },
  "parcelIgnore": [
    "openmct/openmct.js"
  ]
}
